[build]
  command = "npm run build"
  publish = "dist"

# Redirect SPA: todas las rutas sirven index.html (React Router del lado cliente)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
